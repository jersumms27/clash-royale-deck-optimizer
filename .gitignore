# Secrets — never commit the API token (use token.txt.example as a template)
token.txt

# Battle dataset — hundreds of MB once the crawler has run for a while
# (raw battle logs + crawl state, and the flattened CSV; see data/README.md)
data/battles/
data/battles.csv

# Python
__pycache__/
*.py[cod]

# Editor / OS
.DS_Store
